const encryptionKeys = {
    world_of_tanks: [0xDE, 0x72, 0xBE, 0xA0, 0xDE, 0x04, 0xBE, 0xB1, 0xDE, 0xFE, 0xBE, 0xEF, 0xDE, 0xAD, 0xBE, 0xEF],
    world_of_warships: [0x29, 0xB7, 0xC9, 0x09, 0x38, 0x3F, 0x84, 0x88, 0xFA, 0x98, 0xEC, 0x4E, 0x13, 0x19, 0x79, 0xFB]
};

function parser(loadDataMode, debug) {
    this.debug = debug;
    this.loadDataMode = loadDataMode;
  
    if (loadDataMode === loadDataMode_t.bulk) {
      this.loadData();
    }
}

function parse(is, game) {
    const buffer = new Buffer(is);
    parse(buffer, game);
}